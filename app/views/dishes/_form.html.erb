<%= form_with(model: dish) do |form| %>
  <% if dish.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
        <% dish.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <!-- %= form.label :vendor_id, style: "display: block" % -->
    <%= form.number_field :vendor_id, id: :dish_vendor_id, value: current_vendor.id, type: :hidden %>
  </div>

    <!-- %= form.label :dish_id, style: "display: block" % -->
    <!-- %= form.number_field :dish_id % -->
  
  </br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
